# API

The website can call a hosted NeuroBridge API through `/api/adapt`.

- Set `NEUROBRIDGE_API_URL` to enable backend proxy mode.
- If unset, the route uses a local mock adapter for demo reliability.
